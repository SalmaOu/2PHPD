<?php

/*     // On requis le fichier qui permet la connection a la bd

    require("connected_database.php"); 

   // On crée la table 
   $series_table = "CREATE TABLE series_table (
    id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY_KEY,
    title VARCHAR(30) NOT NULL,
    author VARCHAR(30) NOT NULL,
    note VARCHAR(30) NOT NULL,
    created_date DATE NOT NULL,
    )";

    // On vérifie qu'on a bien créé la table

    if ($conn->query($series_table) === TRUE) {
        echo "Table series created successfully";
    } else {
        echo "Error creating table: " . $conn->error;
    }
 */
 
?>

